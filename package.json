{
  "name": "discord-game-presence",
  "version": "1.0.0",
  "description": "Set and auto-rotate Discord game presence using registered application IDs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "auto": "node index.js --auto",
    "auto:fast": "node index.js --auto --interval 2"
  },
  "dependencies": {
    "discord-rpc": "^4.0.1"
  }
}
